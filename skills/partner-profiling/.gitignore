# Python bytecode from the transform scripts.
scripts/__pycache__/

# Seen-partners ledger (run state, if kept inside the skill folder).
*partners_seen.json

# Generated reports. Unlike event-discovery — whose reports are published to the public
# `ersilia-os/digests` repo — these stay LOCAL and are never committed. They contain
# named individuals and contact details; `references/data-handling.md` and this repo's
# public visibility are the reason. Do not `git add -f` a sweep or a dossier.
reports/
