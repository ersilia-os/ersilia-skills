# Python bytecode from the transform scripts.
scripts/__pycache__/

# Seen-events ledger (run state, if kept inside the skill folder).
*events_seen.json
seen_events.json

# Generated report output — kept local as a working copy. The canonical home for
# reports is the remote repo `ersilia-os/digests` (submitted via `gh api`, SKILL.md Step 8).
# Re-commit a specific run locally only if you have a reason:
#   git add -f reports/26-01-01-event-discovery.md
reports/

# Design record / working document — deliberately NOT committed (ROADMAP §6.1).
# It holds the reasoning behind the skill's decisions, including reversals, and is kept
# local by choice. Ignoring it also means a plain `git clean -fd` will not delete it
# (only `-fdx` reaches ignored files). Commit a snapshot deliberately if ever needed:
#   git add -f skills/event-discovery/ROADMAP.md
ROADMAP.md
